<shape xmlns:android="http://schemas.android.com/apk/res/android">
<!--    <solid android:color="@color/tra" /> &lt;!&ndash; Background color &ndash;&gt;-->
    <stroke
        android:width="2dp"
        android:color="@color/fade_gray" /> <!-- Border color and width -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
    <!-- Padding -->
    <corners
        android:topLeftRadius="10dp"
        android:topRightRadius="10dp"
        android:bottomLeftRadius="10dp"
        android:bottomRightRadius="10dp" />
    <!-- Rounded corners -->
</shape>
